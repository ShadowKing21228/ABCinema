<Page x:Class="ABCinema_WPF.Views.HallView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ABCinema_WPF.Views"
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      mah:DialogParticipation.Register="{Binding}"
      mc:Ignorable="d"
      Title="Залы" Background="#2E2E2E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- для шапки -->
            <RowDefinition Height="*"/>    <!-- для контента -->
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="#3E3E3E">
            <Button x:Name="AddHallButton" Command="{Binding ShowHallDialogCommand}" CommandParameter="{Binding}" Margin="20, 10,0,10">
                <TextBlock Text="Добавить зал" FontSize="18"> </TextBlock>
            </Button>
        </StackPanel>
        
        <ListView Grid.Row = "1" ItemsSource="{Binding Halls}" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{Binding Rows, StringFormat=Рядов: {0}}" />
                            <TextBlock Text="{Binding SeatsPerRow, StringFormat=Мест в ряду: {0}}" />
                            <TextBlock Text="{Binding Capacity, StringFormat=Общая вместительность: {0} мест}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
