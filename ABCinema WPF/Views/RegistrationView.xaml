<mah:MetroWindow x:Class="ABCinema_WPF.Views.RegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
        mah:DialogParticipation.Register="{Binding}"
        mc:Ignorable="d"
        Title = "ABCinema - Регистрация" Height="450" Width="800" Background="#2E2E2E" Icon="pack://application:,,,/Resources/Images/Icons/ticketWindow.ico">
    <Grid Margin="100">
        <StackPanel>
            <TextBox controls:TextBoxHelper.Watermark="Имя пользователя" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox controls:TextBoxHelper.Watermark="Электронная почта" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,20,0,0"/>
            <PasswordBox controls:TextBoxHelper.Watermark="Пароль" x:Name="PasswordBox" Margin="0,20,0,0"/>
            <Button Content="Зарегистрироваться" Command="{Binding RegisterCommand}" CommandParameter="{Binding ElementName=PasswordBox}" Margin="0,30,0,0"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
