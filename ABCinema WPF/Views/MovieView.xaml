<Page x:Class="ABCinema_WPF.Views.MovieView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mah:DialogParticipation.Register="{Binding}"
      xmlns:local="clr-namespace:ABCinema_WPF.Views"
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      mc:Ignorable="d"
      Title="Фильмы" Background="#2E2E2E" >
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- для шапки -->
            <RowDefinition Height="*"/>    <!-- для контента -->
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="#3E3E3E">
            <Button x:Name="AddCinemaButton" Command="{Binding ShowAddCinemaDialogCommand}" CommandParameter="{Binding}" Margin="20, 10,0,10">
                <TextBlock Text="Добавить фильм" FontSize="18"> </TextBlock>
            </Button>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding MovieItems}" SelectedItem="{Binding SelectedMovie, Mode=TwoWay}" MouseDoubleClick="Control_OnMouseDoubleClick">
            
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
                
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2E2E2E" CornerRadius="8" Padding="10" Margin="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
            
                                <!-- Постер -->
                                <Image Source="{Binding Poster}" Width="300" Height="600" Margin="0,0,20,0"/>
            
                                <!-- Инфо + кнопки -->
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Id}" TextAlignment="Center" FontSize="12" Foreground="LightGray" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding MovieTitle}" TextAlignment="Center" FontSize="24" Foreground="White" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding Rating}" TextAlignment="Center" FontSize="20" Foreground="Goldenrod" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding DurationMinutes}" TextAlignment="Center" FontSize="18" Foreground="White" Margin="0,0,0,10"/>
                                    <TextBlock Text="{Binding Description}" TextAlignment="Left" TextWrapping="Wrap" Width="80" FontSize="14" Foreground="White" Margin="0,0,0,10"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
