<Page x:Class="ABCinema_WPF.Views.GenreView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ABCinema_WPF.Views"
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      mah:DialogParticipation.Register="{Binding}"
      mc:Ignorable="d"
      Title="Жанры" Background="#2E2E2E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- для шапки -->
            <RowDefinition Height="*"/>    <!-- для контента -->
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal" Grid.Row="0" Background="#3E3E3E">
            <Button x:Name="AddGenreButton" Command="{Binding ShowGenreDialogCommand}" CommandParameter="{Binding}" Margin="20, 10,0,10">
                <TextBlock Text="Добавить жанр" FontSize="18"> </TextBlock>
            </Button>
        </StackPanel>
        
        <ListView Grid.Row = "1" ItemsSource="{Binding Genres}" Margin="10" SelectedItem="{Binding SelectedGenre, Mode=TwoWay}" MouseDoubleClick="ListView_MouseDoubleClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock Text="{Binding Id}" FontWeight="Bold" Foreground="Gray" FontSize="16"/>
                            <TextBlock Text="{Binding Name, StringFormat=Рядов: {0}}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>
