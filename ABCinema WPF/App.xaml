<Application x:Class="ABCinema_WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ABCinema_WPF"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             StartupUri="/Views/MainWindow.xaml">
    
    <Application.Resources>
        
        <ResourceDictionary>
               <ResourceDictionary.MergedDictionaries>
                   <!-- Основные стили -->
                   <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                   <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                   <!-- Цветовая тема -->
                   <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/dark.crimson.xaml" />
               </ResourceDictionary.MergedDictionaries>

        <Style TargetType="mah:MetroWindow">
            <Setter Property="Background" Value="#2E2E2E"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
            
        <Style TargetType="Page" BasedOn="{StaticResource MahApps.Styles.Page}">
            <Setter Property="Background" Value="#2E2E2E"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
            
        <Style x:Key = "TextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource MahApps.Styles.TextBlock}">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="controls:ControlsHelper.CornerRadius" Value="8"/>
        </Style>
            
        <Style TargetType="ListView" BasedOn="{StaticResource MahApps.Styles.ListView}">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        
        <Style TargetType="TextBox" BasedOn="{StaticResource MahApps.Styles.TextBox}">
            <Setter Property="Background" Value="#3a3a3a"/>
            <Setter Property="BorderBrush" Value="DarkGoldenrod"/>
            <Setter Property="controls:ControlsHelper.FocusBorderBrush" Value="Gold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="controls:ControlsHelper.CornerRadius" Value="8"/>
        </Style>
            
            <Style TargetType="mah:DateTimePicker">
                <Setter Property="Background" Value="#3a3a3a"/>
                <Setter Property="BorderBrush" Value="DarkGoldenrod"/>
                <Setter Property="controls:ControlsHelper.FocusBorderBrush" Value="Gold"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="controls:ControlsHelper.CornerRadius" Value="8"/>
            </Style>
        
        <Style TargetType="PasswordBox" BasedOn="{StaticResource MahApps.Styles.PasswordBox}">
            <Setter Property="Background" Value="#3a3a3a"/>
            <Setter Property="BorderBrush" Value="DarkGoldenrod"/>
            <Setter Property="controls:ControlsHelper.FocusBorderBrush" Value="Gold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="controls:ControlsHelper.CornerRadius" Value="8"/>
        </Style>
        
        <Style TargetType="Button" BasedOn="{StaticResource MahApps.Styles.Button}">
             <Setter Property="Background" Value="#8b0927"/>
             <Setter Property="controls:RadioButtonHelper.BackgroundPointerOver" Value="#9d1c39"/>
             <Setter Property="BorderBrush" Value="DarkGoldenrod"/>
             <Setter Property="FontStyle" Value="Normal"/>
             <Setter Property="FontFamily" Value="pack://application:,,,/ABCinema_WPF/Resources/Fonts/#Roboto"/>
             <Setter Property="FontSize" Value="16"/>
             <Setter Property="controls:ControlsHelper.CornerRadius" Value="8"/>
             <Setter Property="controls:ControlsHelper.FocusBorderBrush" Value="Gold"/>
             <Style.Triggers>
                 <DataTrigger Binding="{Binding IsExpired}" Value="True">
                     <Setter Property="Background" Value="#3e3e3e" />
                 </DataTrigger>
             </Style.Triggers>
         </Style>
        </ResourceDictionary>
        
    </Application.Resources>

</Application>
